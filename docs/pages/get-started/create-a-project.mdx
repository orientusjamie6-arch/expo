import React from "react";
import { Text, View, Button, ScrollView, Image, TextInput } from "react-native";

export default function App() {
  return (
    <ScrollView style={{ padding: 20 }}>
      
      {/* Title */}
      <Text style={{ fontSize: 28, fontWeight: "bold", marginBottom: 20 }}>
        Lovely Designs 💐
      </Text>

      {/* Gallery */}
      <Text style={{ fontSize: 20, marginBottom: 10 }}>My Designs</Text>
      
      <Image 
        source={{ uri: "https://via.placeholder.com/300" }} 
        style={{ width: "100%", height: 200, marginBottom: 10 }}
      />
      
      <Image 
        source={{ uri: "https://via.placeholder.com/300" }} 
        style={{ width: "100%", height: 200, marginBottom: 20 }}
      />

      {/* Order Form */}
      <Text style={{ fontSize: 20, marginBottom: 10 }}>Order a Design</Text>
      
      <TextInput 
        placeholder="Your Name"
        style={{ borderWidth: 1, padding: 10, marginBottom: 10 }}
      />
      
      <TextInput 
        placeholder="What do you want?"
        style={{ borderWidth: 1, padding: 10, marginBottom: 10 }}
      />

      <Button title="Submit Order" onPress={() => alert("Order sent!")} />

    </ScrollView>
  );
}
